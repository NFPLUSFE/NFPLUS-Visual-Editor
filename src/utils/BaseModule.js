class BaseModule {
  constructor(name, x, y, height, weight){
    this.name = name
    this.x = x
    this.y = y
    this.height = height
    this.weight = weight
  }
  baseInfo = {
    x: 0,
    y: 0,
    height: -1,
    weight: -1,
  };
}
